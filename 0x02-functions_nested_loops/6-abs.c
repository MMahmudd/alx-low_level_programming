#include "main.h"
/**
 *_abs - computes the absolute value of an integer
 *@n: the int to check
 *Return: the absolute value of int
 *
 *function that computes the absolute value of an integer.
 *
 */
    
int _abs(int n) /*absolure value*/
	{
		if (n >= 0)
		{
			return (n);
		}
		return (-n);
	}
